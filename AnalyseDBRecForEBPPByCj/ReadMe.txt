1.使用jar包可以声明俩月的db导出响应码的txt文档路径，然后merge出当月分析的合并响应码txt文档用于跟上月的对应数据做对比，如不声明会使用默认路径
2.使用的CIAMessage_zh_CN.properties为响应码与响应码描述文件
3.使用的EBPP_Res_SysErr.xls为需求组定义的系统错误响应码Excel
4.程序会产生两个文件，一为合并的txt文档，另外为完整的当月分析原数据